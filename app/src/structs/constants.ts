export const MagicNotPossibleId = Number.MIN_SAFE_INTEGER;

//#region CONSTANTS DUPLICATED IN WRAPPER
// search for this word to find them: APP_WRAPPER_SHARED_CONSTANTS
//!always change variables in both places
export const SilverStandardId = -1;
export const GoldStandardId = -2;
//#endregion

export const HiddenMatchingSolutions = new Set([
  GoldStandardId,
  SilverStandardId,
]);
