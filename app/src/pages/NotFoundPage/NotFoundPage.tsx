import PageStruct from 'components/PageStruct/PageStruct';
import React from 'react';

const NotFoundPage = (): JSX.Element => (
  <PageStruct title="View Not Found" showNextFab={false} />
);

export default NotFoundPage;
