import { IntersectionVennDiagramConfigStrategy } from '../config';

export const IntersectionVennDiagramSelectStrategy: IntersectionVennDiagramConfigStrategy = {
  color: () => undefined,
  opacity: () => undefined,
};
