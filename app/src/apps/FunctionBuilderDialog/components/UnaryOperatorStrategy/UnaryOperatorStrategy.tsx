import { IonChip } from '@ionic/react';
import React from 'react';

const UnaryOperatorStrategy = (): JSX.Element => (
  <>
    <IonChip>UnaryOP</IonChip>(<IonChip>VAL</IonChip>)
  </>
);

export default UnaryOperatorStrategy;
