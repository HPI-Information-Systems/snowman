import { IonChip } from '@ionic/react';
import React from 'react';

const SimilarityThresholdStrategy = (): JSX.Element => (
  <IonChip>THRESHOLD</IonChip>
);

export default SimilarityThresholdStrategy;
